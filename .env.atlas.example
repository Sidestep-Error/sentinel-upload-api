# Atlas mode (use external MongoDB Atlas connection string)

# Keep Nginx on local host for app access.
NGINX_HOST_PORT=8080

# These are only used by the local mongo service. Keep defaults if not used.
MONGO_HOST_PORT=28017
MONGO_ROOT_USERNAME=sentinel
MONGO_ROOT_PASSWORD=change-me
MONGO_DB=sentinel_upload

# Required in Atlas mode:
MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>/<db>?retryWrites=true&w=majority
